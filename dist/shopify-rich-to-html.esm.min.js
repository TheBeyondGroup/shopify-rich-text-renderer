function e(a, u = !1) {
  let h = ''
  if ('root' === a.type && a.children.length > 0)
    h += u
      ? `\n      <div class="${!0 === u ? 'rte' : u}">\n        ${e(
          a.children
        )}\n      </div>\n      `
      : e(a.children)
  else
    for (const e of a)
      switch (e.type) {
        case 'paragraph':
          h += l(e)
          break
        case 'heading':
          h += n(e)
          break
        case 'list':
          h += r(e)
          break
        case 'list-item':
          h += i(e)
          break
        case 'link':
          h += t(e)
          break
        case 'text':
          h += c(e)
      }
  return h
}
function l(l) {
  return l.children ? `<p>${e(l?.children)}</p>` : `<p>${l?.value}</p>`
}
function n(l) {
  return l.children
    ? `<h${l.level}>${e(l.children)}</h${l.level}>`
    : `<h${l.level}>${l.value}</h${l.level}>`
}
function r(l) {
  return l.children ? `<ul>${e(l.children)}</ul>` : `<ul>${l.value}</ul>`
}
function i(l) {
  return l.children ? `<li>${e(l.children)}</li>` : `<li>${l.value}</li>`
}
function t(l) {
  return `<a href="${l.url}" title="${l.title}" target="${l.target}">${e(
    l.children
  )}</a>`
}
function c(e) {
  return e.bold ? `<b>${e.value}</b>` : e.italic ? `<i>${e.value}</i>` : e.value
}
export {
  n as buildHeading,
  t as buildLink,
  r as buildList,
  i as buildListItem,
  l as buildParagraph,
  c as buildText,
  e as richTextToHTML,
}
//# sourceMappingURL=shopify-rich-to-html.esm.min.js.map
